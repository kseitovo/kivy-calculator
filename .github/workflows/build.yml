name: Build APK with Buildozer
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Set up Java JDK 17
      uses: actions/setup-java@v5
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential git ccache \
          python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev \
          libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev \
          libavcodec-dev zlib1g-dev libunwind-dev autoconf libtool pkg-config \
          libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev lld

    - name: Install Python Pip & Buildozer
      run: |
        pip install --upgrade pip
        pip install "cython<3.0" virtualenv buildozer

    - name: Build APK with Buildozer
      env:
        APP_ANDROID_ACCEPT_SDK_LICENSE: "1"
        BUILDOZER_ACCEPT_ANDROID_SDK_LICENSE: "y"
      run: |
        rm -rf ~/.buildozer .buildozer
        
        mkdir -p ~/.android
        touch ~/.android/repositories.cfg
        
        # ФІКС ГІЛКИ PYTHON-FOR-ANDROID: примусово вмикаємо стабільний master замість develop
        sed -i 's/^#\s*p4a.branch =.*/p4a.branch = master/' buildozer.spec
        sed -i 's/^p4a.branch =.*/p4a.branch = master/' buildozer.spec
        
        # Перевірка надійності рядка вимог
        sed -i 's/^requirements =.*/requirements = python3,kivy==2.3.0/' buildozer.spec
        
        buildozer android debug
